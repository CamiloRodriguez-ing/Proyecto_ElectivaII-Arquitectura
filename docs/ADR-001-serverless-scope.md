# ADR-001: Serverless scope

Status: Accepted

The first stage uses only AWS Lambda and API Gateway HTTP API as application resources. Additional services are deferred until persistence or asynchronous processing is authorized.
